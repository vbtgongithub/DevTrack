// Placeholder — DSA Page
import React from 'react';
import { PageShell } from '../components/layout/PageShell';

const DsaPage: React.FC = () => {
  return (
    <PageShell title="DSA Tracker" subtitle="Master your data structures and algorithms" status="success" error={null}>
      <p style={{ color: 'var(--text-muted)' }}>DSA tracker page coming soon...</p>
    </PageShell>
  );
};

export default DsaPage;
