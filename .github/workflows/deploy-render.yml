name: Deploy to Render (Legacy – Superseded)

# This workflow is intentionally DISABLED.
# It has been superseded by deploy-production.yml which handles both
# Vercel (frontend) and Render (backend + workers) in a single, unified pipeline.
#
# Keeping this file so the webhook secret names are documented here:
#   RENDER_API_DEPLOY_HOOK   → devtrack-api service
#   RENDER_SYNC_DEPLOY_HOOK  → devtrack-sync worker
#   RENDER_XP_DEPLOY_HOOK    → devtrack-xp worker
#   RENDER_ORCH_DEPLOY_HOOK  → devtrack-orch worker
#   RENDER_MAINT_DEPLOY_HOOK → devtrack-maint worker
#
# To re-enable this file, change `workflow_dispatch` back to `push: branches: [main]`
# and remove the equivalent job in deploy-production.yml to avoid double-deploys.

on:
  workflow_dispatch:
