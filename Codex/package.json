{
  "name": "devtrack-codex-reference",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DevTrack AI-ready project reference setup scripts",
  "scripts": {
    "install": "npm install --prefix ..",
    "dev": "npm run dev:all --prefix ..",
    "build": "npm run build --prefix ../backend && npm run build --prefix ../frontend",
    "test": "npm run test:unit --prefix ../frontend && npm test --prefix ../backend",
    "lint": "npm run lint --prefix ../frontend"
  }
}
